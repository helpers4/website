import type { SidebarsConfig } from '@docusaurus/types';

const sidebars: SidebarsConfig = {
  docs: [
    'intro',
    'getting-started',
    {
      type: 'category',
      label: 'Actions',
      collapsible: true,
      collapsed: false,
      items: [
        { type: 'autogenerated', dirName: 'actions' },
      ],
    },
  ],
};

export default sidebars;
